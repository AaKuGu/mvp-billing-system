"use client";

import React from "react";
import { Use_Route_Loading_Handler } from "./Use_Route_Loading_Handler";

const Route_Loading_Handler = () => {
  Use_Route_Loading_Handler();
  return null; // nothing to render
};

export default Route_Loading_Handler;
